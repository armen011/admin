import { breakpointPixelValues, breakpointValues } from './breakpoints';
import GlobalStyle from './globals';
import { theme } from './theme';

export { breakpointPixelValues, breakpointValues, GlobalStyle, theme };

export default theme;
